
set(BINARY ${CMAKE_PROJECT_NAME})
file(GLOB_RECURSE SOURCES CONFIGURE_DEPENDS LIST_DIRECTORIES true *.h *.cpp)
if(NOT GLADIUS_ENABLE_OPENCL)
	list(FILTER SOURCES EXCLUDE REGEX ".*/ComputeCore\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/GlobalMortonOctree\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/ManifoldDualContouringGpu\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/ManifoldDualContouringProgram\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/OpenCLComputeRenderer\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/ProgramManager\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/Rendering\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/RenderSceneGeneration\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/RenderSceneState\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/RenderSession\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/GpuKernelAccessGuard\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/HierarchicalDCProgram\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/MeshPreparationProgram\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/OpenClEventRegistry\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/DualContouringGpuSampling\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/DualContouringSamplingProgram\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/OpenCLRenderRequestFactory\\.cpp$")
endif()
target_sources(${BINARY}_lib PRIVATE ${SOURCES})
target_sources(${BINARY}_lib_mcp PRIVATE ${SOURCES})
target_sources(${BINARY} PRIVATE ${SOURCES})

target_include_directories(${BINARY}_lib PRIVATE ./)
target_include_directories(${BINARY}_lib_mcp PRIVATE ./)
target_include_directories(${BINARY} PRIVATE ./)