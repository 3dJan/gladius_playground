
set(BINARY ${CMAKE_PROJECT_NAME})
file(GLOB_RECURSE SOURCES CONFIGURE_DEPENDS LIST_DIRECTORIES true *.h *.cpp)
if(GLADIUS_ENABLE_OPENCL)
	list(FILTER SOURCES EXCLUDE REGEX ".*/RenderWindowWebGPU\\.cpp$")
endif()
if(NOT GLADIUS_ENABLE_OPENCL)
	list(FILTER SOURCES EXCLUDE REGEX ".*/RenderWindow\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/RenderWindowAsync\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/BaseExportDialog\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/CliExportDialog\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/ImageStackExportDialog\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/MeshExportDialog\\.cpp$")
	list(FILTER SOURCES EXCLUDE REGEX ".*/MeshExportDialog3mf\\.cpp$")
endif()
target_sources(${BINARY}_lib PRIVATE ${SOURCES})
target_sources(${BINARY}_lib_mcp PRIVATE ${SOURCES})
target_sources(${BINARY} PRIVATE ${SOURCES})

target_include_directories(${BINARY}_lib PRIVATE ./)
target_include_directories(${BINARY}_lib_mcp PRIVATE ./)
target_include_directories(${BINARY} PRIVATE ./)